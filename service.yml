# apiVersion: v1
# kind: Service
# metadata:
#   name: nginx
# spec:
#   type: NodePort
#   selector:
#     app: nginx #app.kubernetes.io/name: MyApp
#   ports:
#     #- protocol: TCP
#     - port: 80
#       targetPort: 80 #9376
#       nodePort: 30001
apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80