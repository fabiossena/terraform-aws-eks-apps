apiVersion: karpenter.k8s.aws/v1beta1
kind: EC2NodeClass
metadata:
  name: default
spec:
  amiFamily: AL2023

  role: KarpenterNodeRole-SEU_CLUSTER

  subnetSelectorTerms:
    - tags:
        karpenter.sh/discovery: SEU_CLUSTER

  securityGroupSelectorTerms:
    - tags:
        karpenter.sh/discovery: SEU_CLUSTER

  tags:
    karpenter.sh/discovery: SEU_CLUSTER